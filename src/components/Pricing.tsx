import React from 'react';
import { Pricing as PricingCards } from './ui/pricing-cards';

const Pricing: React.FC = () => {
  return (
    <section id="pricing">
      <PricingCards />
    </section>
  );
};

export default Pricing;